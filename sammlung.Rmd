---
title: "Gesammelte 2-Euro-Münzen"
author: "Henning & Thomas"
date: "erst. `r Sys.Date()`"
output:
  html_document:
    theme: readable
params:
  coins: NULL
  collection: NULL
  circulation: NULL
---
Willkommen in unserer Sammlung von *2-Euro-Münzen* – einer Chronik der europäischen Kultur, Geschichte und Politik, geprägt in Kupfer, Nickel und Zink.  
Diese Aufstellung dokumentiert unsere gemeinsame Anstrengung beim Finden neuer Motive im täglichen Umgang mit Münzgeld und bietet damit einen Überblick über die bereits gefundenen und der Sammlung hinzugefügten Stücke.  
Nicht zuletzt ist sie ein Spiegelbild der europäischen Vielfalt – festgehalten auf dem kleinsten gemeinsamen Nenner unseres Kontinents.


```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)

## Libraries
library(tidyverse)

## Daten beziehen: Aus Params (aus Shiny) oder Fallback (manuell knitten)
if (!is.null(params$coins)) {
  coins <- params$coins
  collection <- params$collection
  circulation <- params$circulation
} else {
  # Fallback, falls Rmd direkt in RStudio geknittet wird:
  # 'local = TRUE' verhindert Seiteneffekte in der globalen Umgebung
  source("rd_celex.r", local = knitr::knit_global())       # celex
  source("rd_collection.r", local = knitr::knit_global())  # collection
  source("rd_circulation.r", local = knitr::knit_global()) # circulation
}
```

<style type="text/css">
h1.title {
  text-align: center;
}
h4 {
  text-align: center;
}
</style>

```{r data, echo=FALSE}
# Sicherheits-Check: Falls die Skripte abweichende Variablennamen nutzen
df <- Reduce(function(...) merge(..., by = "ID", all.x = TRUE, no.dups = TRUE),
             list(coins, collection |> select(ID, Qualität, Ablage), circulation)) |> 
  as_tibble() |> 
  filter(!is.na(Ablage)) |> # nur noch nicht gesammelte
  transmute(Jahr = paste0(str_dup(paste0(Prägejahr, "       ·       "), 7), Prägejahr),
            Land,
            Mzz = abbreviate(Münzzeichen, 7), 
            Motiv = case_when(nchar(Abbildung) > 70 ~ paste0(str_sub(Abbildung, 1, 70), "..."),
                             TRUE ~ Abbildung),
            Qualität, Hfgkt) |> 
  mutate(Qualität = case_when(Qualität == 0 ~ "(0) ★★★", # Geschütztes Leerzeichen (Alt + 0160)
                              Qualität == 1 ~ "(1) ☆★★",
                              Qualität == 2 ~ "(2) ☆☆★",
                              Qualität == 3 ~ "(3) ☆☆☆"),
         Hfgkt = c("▼", "▽", "▢", "△", "▲")[Hfgkt]) |>
  arrange(Jahr, Land, Motiv, Mzz) |> 
  group_by(Jahr)
```

```{r table}
gt::gt(df, id = "coin_coll") |> 
  # gt::data_color(columns = "Qualität", target_columns = everything(),
  #                palette = "YlGn", alpha = .5, reverse = TRUE, method = "factor",
  #                contrast_algo = "wcag") |>  
  gt::cols_align(align = "right", columns = "Mzz") |> 
  gt::cols_align(align = "center", columns = "Hfgkt") |> 
  gt::tab_options(table.font.size = 11,
                  row_group.background.color = "grey45",
                  row_group.font.weight = "bold",
                  row_group.padding = 2,
                  column_labels.padding = 2,
                  data_row.padding = 1)
```

<!-- Manuell ausführen mit:
rmarkdown::render(input = "sammlung.Rmd", output_dir = getwd())
-->